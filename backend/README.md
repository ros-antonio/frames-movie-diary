# Backend

This folder contains the Express + Prisma backend for Frames Movie Diary.

For setup, environment files, development, testing, and build instructions, use the root `README.md`.

Main backend entry points:

- `src/server.ts`
- `src/app.ts`
- `src/routes/`
- `prisma/schema.prisma`
